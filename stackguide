#!/usr/bin/env bash

# StackGuide - Local-first AI Knowledge Assistant
# Main entry point script for easy command execution

set -e

# Colors for output
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# Script directory
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Function to show help
show_help() {
    echo -e "${BLUE}🚀 StackGuide - Local-first AI Knowledge Assistant${NC}"
    echo ""
    echo -e "${GREEN}Available Commands:${NC}"
    echo "  start           - Start development environment and open CLI"
    echo "  start-gpu       - Start development environment with GPU support"
    echo "  cli             - Open interactive CLI"
    echo "  ingest          - Ingest data from local sources"
    echo "  ingest-url      - Ingest specific URLs (Confluence, Notion, GitHub)"
    echo "  query <text>    - Run a specific query"
    echo "  status          - Show system status and health"
    echo "  logs            - View all service logs"
    echo "  stop            - Stop all services"
    echo "  restart         - Restart all services"
    echo "  clean           - Clean up containers and images"
    echo "  help            - Show this help message"
    echo ""
    echo -e "${YELLOW}Examples:${NC}"
    echo "  stackguide start"
    echo "  stackguide ingest-url"
    echo "  stackguide query 'How do I set up the database?'"
    echo "  stackguide status"
}

# Function to check if Docker is running
check_docker() {
    if ! docker info > /dev/null 2>&1; then
        echo -e "${RED}❌ Docker is not running. Please start Docker and try again.${NC}"
        exit 1
    fi
}

# Function to check if services are running
check_services() {
    if ! docker compose ps | grep -q "Up"; then
        echo -e "${YELLOW}⚠️  Services are not running. Starting them...${NC}"
        return 1
    fi
    return 0
}

# Function to start services
start_services() {
    echo -e "${BLUE}🚀 Starting StackGuide services...${NC}"
    cd "$SCRIPT_DIR"
    
    if [ "$1" = "gpu" ]; then
        echo -e "${BLUE}🔧 Using GPU profile...${NC}"
        docker compose -f docker-compose.dev.yml --profile gpu up --build -d
    else
        echo -e "${BLUE}🔧 Using CPU profile...${NC}"
        docker compose -f docker-compose.dev.yml --profile cpu up --build -d
    fi
    
    echo -e "${YELLOW}⏳ Waiting for services to be ready...${NC}"
    sleep 15
    
    if check_services; then
        echo -e "${GREEN}✅ Services are running!${NC}"
        return 0
    else
        echo -e "${RED}❌ Failed to start services. Check logs with 'stackguide logs'${NC}"
        return 1
    fi
}

# Function to open CLI
open_cli() {
    echo -e "${BLUE}💻 Opening StackGuide CLI...${NC}"
    cd "$SCRIPT_DIR"
    docker compose exec api python -m cli.main
}

# Function to run a query
run_query() {
    if [ -z "$1" ]; then
        echo -e "${RED}❌ Please provide a query. Usage: stackguide query 'your question here'${NC}"
        exit 1
    fi
    
    echo -e "${BLUE}🔍 Running query: $1${NC}"
    cd "$SCRIPT_DIR"
    
    # Check if services are running, start if needed
    if ! check_services; then
        start_services
    fi
    
    # Run the query
    docker compose exec api python -c "
from core.knowledge import KnowledgeEngine
engine = KnowledgeEngine()
response = engine.query('$1')
print('\\n📚 Answer:')
print(response.answer)
print(f'\\n🎯 Confidence: {response.confidence:.2f}')
if response.sources:
    print('\\n📖 Sources:')
    for i, source in enumerate(response.sources[:3], 1):
        print(f'{i}. {source.source} (Relevance: {source.score:.2f})')
"
}

# Function to show status
show_status() {
    echo -e "${BLUE}📊 StackGuide Status${NC}"
    echo "=================="
    cd "$SCRIPT_DIR"
    
    # Check if services are running
    if check_services; then
        echo -e "${GREEN}✅ Services are running${NC}"
        echo ""
        echo -e "${BLUE}📋 Service Details:${NC}"
        docker compose ps
        echo ""
        echo -e "${BLUE}🔍 Health Check:${NC}"
        docker compose exec api python -c "
from cli.main import run_status
run_status()
"
    else
        echo -e "${RED}❌ Services are not running${NC}"
        echo "Use 'stackguide start' to start the services"
    fi
}

# Function to show logs
show_logs() {
    echo -e "${BLUE}📋 StackGuide Logs${NC}"
    cd "$SCRIPT_DIR"
    docker compose logs -f
}

# Function to stop services
stop_services() {
    echo -e "${YELLOW}🛑 Stopping StackGuide services...${NC}"
    cd "$SCRIPT_DIR"
    docker compose down
    echo -e "${GREEN}✅ Services stopped${NC}"
}

# Function to restart services
restart_services() {
    echo -e "${YELLOW}🔄 Restarting StackGuide services...${NC}"
    stop_services
    sleep 2
    start_services
}

# Function to clean up
clean_up() {
    echo -e "${YELLOW}🧹 Cleaning up StackGuide...${NC}"
    cd "$SCRIPT_DIR"
    docker compose down -v --remove-orphans
    docker system prune -f
    echo -e "${GREEN}✅ Cleanup complete${NC}"
}

# Main script logic
main() {
    # Check if Docker is running
    check_docker
    
    case "${1:-help}" in
        "start")
            start_services
            if [ $? -eq 0 ]; then
                echo -e "${GREEN}🎉 StackGuide is ready!${NC}"
                echo -e "${BLUE}💡 Use 'stackguide cli' to open the interactive CLI${NC}"
            fi
            ;;
        "start-gpu")
            start_services "gpu"
            if [ $? -eq 0 ]; then
                echo -e "${GREEN}🎉 StackGuide is ready with GPU support!${NC}"
                echo -e "${BLUE}💡 Use 'stackguide cli' to open the interactive CLI${NC}"
            fi
            ;;
        "cli")
            if ! check_services; then
                echo -e "${YELLOW}⚠️  Starting services first...${NC}"
                start_services
            fi
            open_cli
            ;;
        "ingest")
            if ! check_services; then
                echo -e "${YELLOW}⚠️  Starting services first...${NC}"
                start_services
            fi
            echo -e "${BLUE}📚 Starting data ingestion...${NC}"
            cd "$SCRIPT_DIR"
            docker compose exec api python -c "
from cli.main import run_ingestion
run_ingestion()
"
            ;;
        "ingest-url")
            if ! check_services; then
                echo -e "${YELLOW}⚠️  Starting services first...${NC}"
                start_services
            fi
            open_cli
            ;;
        "query")
            shift
            run_query "$*"
            ;;
        "status")
            show_status
            ;;
        "logs")
            show_logs
            ;;
        "stop")
            stop_services
            ;;
        "restart")
            restart_services
            ;;
        "clean")
            clean_up
            ;;
        "help"|"--help"|"-h")
            show_help
            ;;
        *)
            echo -e "${RED}❌ Unknown command: $1${NC}"
            echo ""
            show_help
            exit 1
            ;;
    esac
}

# Run main function with all arguments
main "$@"
